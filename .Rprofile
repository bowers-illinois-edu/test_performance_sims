options(repos = structure(c(CRAN = "https://cloud.r-project.org/")))

## Only use renv on the laptop where I'm working on multiple projects at a time
hostname <- Sys.info()["nodename"]
if(hostname=="PS-JWBOWERS-P10G94271"){
        source("renv/activate.R")
}

